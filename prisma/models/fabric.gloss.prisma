model FabricGloss {
    id          Int      @id @default(autoincrement())
    description String   @db.VarChar(100) @unique

    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
    @@map("fabric_gloss")
    Fabric Fabric[]
}