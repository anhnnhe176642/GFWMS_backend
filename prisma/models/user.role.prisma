model Role {
    name String  @id @db.VarChar(10)
    description String? @unique @db.VarChar(15)
    users User[]
    rolePermissions  RolePermission[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
