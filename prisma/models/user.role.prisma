model Role {
    name String  @id @db.VarChar(10)
    fullName String? @unique @db.VarChar(15)
    description String? @db.VarChar(255)
    users User[]
    rolePermissions  RolePermission[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
